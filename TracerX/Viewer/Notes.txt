Time formats
		TZ
			utc
			logger tz
			viewer tz
		format
			show year
			show milliseconds
			24 vs. 12 hour format
			
Hide/show/bookmark methods
	
File menu
	Save as text/xml/excel
	
Find/filter by text/regex/wildcard
	
Find dialog - support wildcards and regular exceptions, next/prev exception.

Auto-refresh (e.g. every 5 seconds).
Add "assert" logging similer to AntiCopy tracer.cs	
//Display long lines in tooltip when mouse hovers.
Option to display function call duration on each "exiting" line.
//Set a bit on lines that contain exceptions.	
Logger filter should support hierarchy somehow.
Method filter should allow callees to be shown/hidden.


	
	